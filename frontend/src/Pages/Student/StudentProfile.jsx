import React, { useState, useEffect } from 'react';

export default StudentProfile(){
const [student, setStudent] = useState({ name: "Guest", class: "N/A", rollNo: "000" });
const navigate = useNavigate();
    return(

    );
}